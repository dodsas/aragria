# Command — 명령 입력 정책

이 문서는 클라이언트에서 들어오는 `cmd` 메시지에 대해 서버가 적용하는 입력 제한(rate limit)을 정의한다. 게임 로직은 `server/game.js`, 튜너블 값은 `server/config.js` 에 있다.

## 권한 모델

- **서버가 단일 권한이다.** 모든 입력 제한은 서버에서 강제한다. 클라이언트는 UX 목적(쿨다운 표시 등)으로만 보조 제한을 둘 수 있다.
- 클라이언트가 보낸 타임스탬프, 카운터, 쿨다운 상태는 **신뢰하지 않는다**. 서버는 자체 시계(`Date.now()`)와 플레이어별 상태만으로 판정한다.
- 따라서 클라이언트 코드 변조(개발자 도구, 패치된 빌드, 직접 WebSocket 호출)로 제한을 우회할 수 없다.

## 명령별 제한

### `attack` — 공격 쿨다운

- **간격:** 같은 플레이어가 연속해서 공격할 수 있는 최소 간격은 `ATTACK_COOLDOWN_MS` (기본 `1000ms`).
- **적용 시점:** `Game.attack(player, arg)` 진입 직후, 대상 탐색 **전**에 검사한다. 대상이 없거나 자기 자신이거나 잘못된 인자라도 쿨다운 타이머는 갱신된다 — 클라이언트가 빈 인자로 무한 핑하는 것을 막기 위함.
- **거부 응답:** `type: 'system'` 메시지로 남은 시간(ms)을 알린다. 이미 진행 중인 전투 패널은 건드리지 않는다.
- **상태 저장 위치:** `player.lastAttackAt` (서버 메모리). 플레이어 접속 시 0으로 초기화. 끊긴 후 재접속하면 새 플레이어 객체가 생성되어 자동 초기화된다 — 재접속을 빠른 재공격에 악용할 수 없게 하려면 IP/계정 단위로 옮겨야 하지만, 현재 스켈레톤은 세션 단위 ID로 충분하다고 본다.

### 그 외 명령

- `move`, `look`, `say`, `use`, `help` 등은 현재 명시적 rate limit이 없다.
- WASD 이동은 키 리피트로 초당 20~30건이 들어올 수 있지만, 서버 측 throttle은 아직 도입하지 않았다 (`movement.md` 참고). 부하 측정 후 결정.

## 1k 동접 관점

- 공격 쿨다운은 플레이어당 1초 = 초당 최대 1회. 1k 동접 × 1 atk/s = 1k atk/s 가 상한이다. 메시지·이벤트 루프 부하 모두 안전한 범위.
- 쿨다운 검사는 `Date.now()` 한 번 + 정수 비교 한 번 — O(1). 핫패스 비용 없음.
- 쿨다운 거부 시에도 시스템 메시지 한 줄을 응답한다. 클라이언트가 의도적으로 쿨다운을 무시하고 1ms 간격으로 보내도 서버가 즉시 거부 응답을 보내므로, 트래픽 증폭은 1:1 수준에서 멈춘다. 의도적 어뷰저는 별도 abuse 처리 계층(연결 종료 등)에서 다룬다 — 현재는 미구현.

## 튜너블

```js
// server/config.js
export const ATTACK_COOLDOWN_MS = 1000;
```

값은 `config.js` 에서만 수정한다. `game.js` 는 직접 숫자를 박지 않는다.

## 새 명령에 rate limit을 추가할 때

1. `server/config.js` 에 `<COMMAND>_COOLDOWN_MS` 상수 추가.
2. 플레이어 객체에 `last<Command>At` 필드 추가, `addPlayer` 에서 0 초기화.
3. 해당 명령의 dispatcher 진입부에서 `Date.now() - player.last<Command>At < COOLDOWN` 검사, 거부 시 `system` 메시지로 응답하고 return.
4. 통과한 경우 `player.last<Command>At = Date.now()` 갱신.
5. 본 문서 "명령별 제한" 절에 항목 추가.

쿨다운 외 다른 형태(토큰 버킷, 슬라이딩 윈도우 등)가 필요해지면 별도 헬퍼로 빼고 본 문서에서 그 동작을 명시한다.

## 변경 이력

- `attack` 1초 쿨다운 (서버 강제) 도입
