# class.md — 직업 시스템

Agria 의 모든 캐릭터는 직업(`klass`)을 가진다. 직업은 캐릭터의 자원 풀(MP),
HP/공격/방어 성장 곡선, 사용 가능한 명령(예: 마법 시전), 그리고 추후의 직업
고유 스킬을 결정한다.

직업 정의는 서버 단일 진실원: `server/game.js` 의 `CLASS_DEFS`. 클라이언트는
`status.klass` / `status.klassName` 을 받아 사이드바 캐릭터 블록과 모바일
액션 패드의 카테고리 노출 여부를 결정한다.

---

## 직업 매트릭스

| id        | 표시명     | hpAtLv1 | hpPerLv | mpAtLv1 | mpPerLv | atkBonus | defBonus | 컨셉                                |
|-----------|------------|---------|---------|---------|---------|----------|----------|--------------------------------------|
| `novice`  | 초보자     | 100     | 10      | 0       | 0       | 0        | 0        | 출발 직업. 모두 novice 로 시작.       |
| `warrior` | 전사       | 130     | 14      | 0       | 0       | +3       | +2       | 탱커. 가장 두꺼운 HP + 근접 공방.      |
| `mage`    | 마법사     | 80      | 8       | 30      | 5       | 0        | 0        | 가장 약한 HP, 큰 마법 데미지로 보상.   |
| `thief`   | 도둑       | 90      | 9       | 0       | 0       | +5       | 0        | 종이 방어 + 가장 큰 패시브 공격.      |
| `archer`  | 궁수       | 100     | 10      | 0       | 0       | +4       | 0        | 안정적 HP + 큰 패시브 공격.           |
| `healer`  | 힐러       | 95      | 10      | 35      | 6       | 0        | +1       | 가장 큰 MP 풀. 공격 약함, 방어 +1.    |
| `bard`    | 음유시인   | 100     | 10      | 25      | 4       | +1       | +1       | 만능형. 모든 축에 작은 보정.          |

**maxHp 공식:** `hpAtLv1 + (level - 1) * hpPerLv` — `classMaxHp(klass, level)`
한 곳에서 계산. 옛 버전(직업 무관 공통 100+10/lv) 에서 직업별 차별화로 일반화.

**maxMp 공식:** `mpAtLv1 + (level - 1) * mpPerLv` — `classMaxMp(klass, level)`.

**atk/defBonus:** `_totalAttack(player)` / `_totalDefense(player)` 가 장비
합 외에 더하는 직업 패시브. 빈손 전사 / 도둑 / 궁수가 빈손 마법사보다 「먼저
때린다」는 감각의 단일 진실원.

### 레벨 30 시점 환산

| 직업       | maxHp | maxMp |
|------------|-------|-------|
| 초보자     | 390   | 0     |
| 전사       | 536   | 0     |
| 마법사     | 312   | 175   |
| 도둑       | 351   | 0     |
| 궁수       | 390   | 0     |
| 힐러       | 385   | 209   |
| 음유시인   | 390   | 141   |

---

## 직업별 특징

### 전사 (warrior)
- 가장 두꺼운 HP — L30 기준 노비스보다 +146 HP.
- 큰 atk(+3) 와 def(+2) 패시브로 빈손도 위협적이고, 갑옷 효율이 좋다.
- 마법 자원 없음. 후속 「강타」 같은 액티브 스킬은 향후 작업.
- 권장 빌드: 갑옷 무거운 슬롯 + 단순 무기. 두 발자국 늦지만 끝까지 살아남는다.

### 마법사 (mage)
- 가장 약한 HP — L30 기준 노비스보다 -78 HP.
- 마법 데미지(파이어볼·아이스 애로우·번개·해골 전사·메테오) 로 보상 — `magic.md` 참조.
- MP 회복 3 초 주기 1 포인트(`_regenTick` 의 mpRegen 분기).
- 권장 빌드: 후방에서 시전, MP 풀이 다 차면 메테오로 큰 그림.

### 도둑 (thief)
- 종이 방어(HP 90 + 9/lv) + 가장 큰 atk 패시브(+5) — 「먼저 때리고 먼저 죽는」 빌드.
- 마법 자원 없음. 후속 「독공격」(DoT) / 「은신」(회피) 같은 액티브는 향후 작업.
- 권장 빌드: 가벼운 방어 슬롯 포기하고 공격 + 회피 우선. 첫 일격으로 끝낸다.

### 궁수 (archer)
- 안정적 HP(노비스 동일) + 큰 atk 패시브(+4) — 도둑보다 한 단계 안전하지만 한 단계 느린 결정타.
- 마법 자원 없음. 후속 「조준 사격」(크리티컬) 같은 액티브는 향후 작업.
- 권장 빌드: 활 슬롯 + 가벼운 방어. 거리감을 두고 안전하게 깎아 내려간다.

### 힐러 (healer)
- 가장 큰 MP 풀(35 + 6/lv, L30 기준 209) — 회복 마법 비용을 감당하기 위함.
- 공격 패시브 없음, 방어 +1 — 직접 전투엔 약하지만 후방 안전성이 약간 있다.
- 후속 「힐」(자기/같은 룸 회복), 「큐어」(상태이상 해제) 같은 액티브는 향후 작업.
- 권장 빌드: 갑옷·로브 + 마나 풀이 다 차도록 천천히 깎는 동료와 동행.

### 음유시인 (bard)
- 모든 축에 작은 보정(HP 노비스 동일, MP 25+4/lv, atk +1, def +1) — 만능형이지만 정점 없음.
- 후속 「영감의 노래」(같은 룸 atk 버프), 「자장가」(몬스터 행동 둔화) 같은 액티브 메커닉이
  들어오면 진짜 가치는 거기서 나온다 — 현재는 「치우치지 않은」 분기로만 의미 있는 차별화.
- 권장 빌드: 어떤 슬롯이든 무난, 같은 룸의 동료와 함께 갈 때 최대치.

---

## 전직 규칙

세 조건이 모두 충족되어야 `전직 <직업>` 명령이 통과한다:

1. `player.klass === 'novice'` — 이미 전직한 캐릭터는 재전직 불가.
2. `player.level >= 10` — 임계 미달이면 시스템 라인으로 거부 사유 안내.
3. `player.roomId === 'square'` — 광장(town/square) 외 다른 방에서는 거부.

성공 시:

- `klass` 갱신.
- `maxHp / maxMp` 재계산 → `hp / mp` 가득 채움(전직 보상 회복).
- mp 자원 사용군(mage/healer/bard) 이면 `Game.mpRegen` 인덱스에 add — 그 외는 delete.
- 시스템 라인: 「{직업명}로 전직했다. 광장의 마법진이 푸르게 타오른다.」
- 같은 방의 다른 플레이어에게 브로드캐스트 라인 송출.
- 마법사로 전직 시 첫 마법(레벨 10 해금) 안내 라인 한 줄. 다른 직업은 현재
  고유 스킬이 없어 별도 안내 없음(향후 「강타」/「힐」 등이 들어오면 동등 안내).

명령 진입점: `Game.changeClass(player, raw)` (server/game.js).
허용 입력 매트릭스(`CHANGE_CLASS_ALIASES`):

| 입력           | → klass id  |
|----------------|--------------|
| 전사 / warrior   | warrior      |
| 마법사 / mage    | mage         |
| 도둑 / thief     | thief        |
| 궁수 / archer    | archer       |
| 힐러 / healer    | healer       |
| 음유시인 / bard  | bard         |

---

## 레벨 / 경험치

`server/game.js` 의 `EXP_TABLE` 이 누적 경험치 임계 표(인덱스 = 레벨-1).
현재 캡 = `MAX_LEVEL = 30`. L11 부터 Δ가 380→760 까지 +20 씩 늘어나는
산술 progression.

| Lv | 누적 EXP | 누적 EXP 차 |
|----|----------|-------------|
| 1  | 0        | -           |
| 2  | 100      | 100         |
| 5  | 520      | 160         |
| 10 | 1620     | 260         |
| 15 | 3720     | 460         |
| 20 | 6320     | 560         |
| 25 | 9420     | 660         |
| 30 | 13020    | 760         |

처치 보상은 `MONSTER_DEFS[def].expReward`. 마지막 일격을 가한 한 명만
`_grantExp` 를 통해 받는다(킬스틸 패시브 분배 없음). 누적 exp 가 다음 임계를
넘으면 자동 레벨업:

- `maxHp / maxMp` 재계산.
- `hp / mp` 가득 채움.
- 「▲ 레벨 N 달성」 시스템 라인.
- 새로 해금된 마법이 있으면 「새 마법을 익혔다 — 파이어볼」 한 줄씩(마법사 한정).
- 레벨 10 도달 + novice + 광장 위치면 6 직업 분기 안내 라인 추가.

레벨 캡(`MAX_LEVEL`)을 넘는 exp 누적은 무시 — `EXP_TABLE` 길이를 늘리면
캡이 자동으로 따라 올라간다.

---

## 향후 스킬 TODO

직업 차별화는 현재 stat(HP/MP/atk/def) 만으로 표현된다. 다음 단계는 직업
고유 액티브 스킬:

- **전사 「강타」** — 쿨다운 ~10s, 큰 일격 + 방어 무시 일부.
- **전사 「방패 막기」** — 패시브? 액티브? 들어오는 데미지 한 번 무효.
- **도둑 「독공격」** — DoT(시간 경과 데미지). 몬스터에 status effect 부착.
- **도둑 「은신」** — 짧은 회피 윈도우, 다음 일격에 크리티컬.
- **궁수 「조준 사격」** — 시전 시간 후 큰 일격. 시전 중 이동 불가.
- **궁수 「관통 사격」** — 룸의 다중 적에게 데미지 분산.
- **힐러 「힐」** — 자기 또는 같은 룸 동료 회복. spell-kind 확장(`SPELL_DEFS.kind: 'heal'`).
- **힐러 「큐어」** — 상태이상 제거. status effect 시스템과 짝.
- **음유시인 「영감의 노래」** — 같은 룸 동료 atk 버프(timer-based).
- **음유시인 「자장가」** — 몬스터 atk 둔화.

스킬 시스템이 들어올 때 함께 도입할 인프라:

- 상태이상(status effect) 큐 — `player.statusEffects: [{ kind, expiresAt, magnitude }]`.
- 쿨다운 슬롯 — 직업별 스킬마다 별도 쿨다운 트랙.
- 클라 액션 패드 — 직업별 「스킬」 카테고리 자동 채우기.
- `command.md` 의 server-authoritative cooldown 매트릭스 갱신.

---

## 새 직업 추가 절차

1. `server/game.js` 의 `CLASS_DEFS` 에 엔트리 추가
   (name/desc/hpAtLv1/hpPerLv/mpAtLv1/mpPerLv/atkBonus/defBonus).
2. `PLAYABLE_CLASSES` 배열에 id 추가(전직 안내 라인의 단일 진실원).
3. `CHANGE_CLASS_ALIASES` 에 한국어 직업명/영문 id 양쪽 등록.
4. 직업 고유 명령(예: 마법사의 spell 시전, 전사의 강타 등)은 별도 명령
   메서드를 만들고 `handleCommand` 디스패처에 추가. spell 처럼 다단어
   명령이 필요하면 prefix matcher(`_matchSpellPrefix` 패턴)를 참고.
5. 본 문서의 직업 매트릭스 표에 한 줄 추가.
6. UI 가 직업별로 분기되어야 한다면(예: 마나 행 노출), 클라이언트
   `renderStatus` 에서 `status.klass` / `maxMp` 분기로 처리.
7. `tests/game.test.js` 의 「직업 시스템 회귀 가드」 절에 changeClass /
   maxHp / maxMp / atk·def / mpRegen 5 가지 검증 추가.

---

## 클라이언트 표시

사이드바 캐릭터 블록(`#character-stats`)이 다음을 보여 준다:

- 직업 (klassName)
- 레벨 (Lv. N)
- 체력 바 — 항상 표시
- 마나 바 — `maxMp > 0` 일 때만 표시(novice/warrior/thief/archer 자동 숨김)
- 경험치 바 — 만렙은 「MAX」 텍스트로 고정
- 전직 안내 — `canChangeClass=true` 일 때 강조 박스 노출

`canChangeClass` 는 서버에서 권위 계산 후 보내준다(클라가 광장 좌표/레벨을
알 필요 없도록). 클라는 단순히 boolean 만 보고 박스를 켜고 끈다.

---

## 변경 이력

- **새 직업 5종 추가(전사/도둑/궁수/힐러/음유시인).** 옛 매트릭스(novice/mage)
  를 6 직업 + novice 출발 직업으로 확장. 각 직업이 hp/mp/atk/def 4 축에서
  차별화. `classMaxHp` 가 직업별 `hpAtLv1`/`hpPerLv` 를 읽도록 일반화. `mages`
  Set 을 `mpRegen` 으로 rename — mp 자원 사용군(mage/healer/bard) 의 일반
  인덱스로 의미 확장. 회귀 가드 6 케이스(transition 매트릭스 / classMaxHp /
  classMaxMp / mpRegen 등록 / atk·def bonus 적용) 추가, 85/85 통과.

  마이그레이션 노트: 기존 mage 캐릭터의 saved.maxHp 는 옛 공식(100+10/lv) 으로
  저장됐다. hydrate 는 `saved.maxHp || classMaxHp(...)` 로 saved 값을 보존하지만,
  레벨업 시 `classMaxHp(player.klass, after)` 로 새 공식을 적용 — 다음 레벨업
  부터 점진적으로 새 공식(80+8/lv) 으로 수렴한다. 이는 의도된 「마법사
  squishy」 디자인이고, HP 가 한 번 줄어들 때 클라가 즉시 보이게 된다.

- 향후 스킬 TODO 절 신설 — 5 직업 모두 액티브 스킬 인프라가 들어와야 진짜
  차별화가 완성된다는 점을 명시. 현재는 패시브 스탯만으로 의미 있는 분기.
