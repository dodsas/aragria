services:
  - type: web
    name: aragria
    runtime: node
    plan: free
    region: singapore
    branch: develop
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
