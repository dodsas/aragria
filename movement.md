# Movement — 이동 시스템

## 개요

이동 명령은 두 경로로 들어온다.

1. **텍스트 명령** — `go north`, `n`, `북` 등.
2. **이동 모드 (WASD)** — 키 한 번에 한 방향 이동.

서버 측에서는 두 경로 모두 동일한 `Game.move(player, dir)` 로 합쳐진다 — 즉, 클라이언트 단축키는 **서버에 새 프로토콜을 추가하지 않고** 기존 `cmd` 메시지로 `go <direction>` 을 보내는 얇은 래퍼다. 서버는 모드 개념을 모른다.

## 모드

클라이언트에는 두 가지 입력 모드가 있다.

| 모드 | 표시 | 입력창 | 키 동작 |
| --- | --- | --- | --- |
| `typing` | 배지 `TYPING`, 프롬프트 `>` | 활성, 포커스 | 텍스트 입력 → Enter로 명령 전송 |
| `move`   | 배지 `MOVE` (강조색), 프롬프트 `✥` | 비활성(`disabled`), 블러 | WASD → 방향 이동 |

전환은 **ESC 토글** 한 가지 방법뿐이다. 어느 모드에서 ESC를 눌러도 반대 모드로 바뀐다.

추가로 **Enter 키는 항상 입력 모드(typing)로 들어가게 동작**한다. 포커스가 어디에 있든(맵, 사이드바, body 등) Enter를 누르면 프롬프트 입력창에 포커스가 잡힌다. `move` 모드에서는 `typing`으로 전환하고, `typing` 모드에서 프롬프트가 포커스를 잃은 상태라면 프롬프트로 포커스를 되돌린다 — 프롬프트가 이미 포커스를 갖고 있으면 form submit이 그대로 진행된다.

### 키 매핑

```
W → north (북)
A → west  (서)
S → south (남)
D → east  (동)
```

표준 게임 키 배치를 따른다. 사용자가 "동서남북" 순으로 적었더라도 매핑은 위 표가 정답.

### 동작 규칙

- `move` 모드에서 modifier(`Ctrl/Cmd/Alt`) 가 함께 눌리면 **무시** — 브라우저 단축키와 충돌 방지.
- `move` 모드에서 WASD 외 키는 무시. ESC만 모드 전환.
- WASD 입력 시 로그에 `> [이동] <방향>` 한 줄을 echo로 남겨 사용자가 자기 입력을 확인할 수 있게 한다.
- 모드 진입 시 입력창의 잔여 텍스트는 비운다 — 다음에 typing으로 돌아왔을 때 이전 입력이 남아 혼란스럽지 않도록.

## 구현 위치

- **클라이언트:** `public/client.js`
  - 상수 `WASD_DIR` — `e.code` 기반 물리 키 위치 → 방향 이름 매핑 (`KeyW/A/S/D`). `e.key` 대신 `e.code`를 사용해 한국어 IME나 키보드 레이아웃과 무관하게 동작.
  - 상태 변수 `mode` — `'typing' | 'move'`.
  - `setMode(next)` — DOM(`body[data-mode]`, 배지, 프롬프트 글리프, 입력 disabled/focus) 및 `document.body.focus()`를 한 곳에서 갱신. 이동 모드 진입 시 `body`에 포커스를 명시적으로 넘겨 키이벤트 전달을 보장.
  - `document.keydown` 글로벌 리스너 — ESC 토글, 그리고 move 모드에서만 WASD 디스패치.
- **CSS:** `public/style.css`
  - `body[data-mode="move"]` 셀렉터 아래에 시각 큐(프롬프트 글리프 색, 가장자리 그라데이션) 정의.
  - `#mode-badge.mode-typing` / `.mode-move` — 배지 색 토글.
- **서버:** 변경 없음. 기존 `Game.move()` 가 `north/south/east/west` 와 한국어/영문 단축어를 모두 처리하므로 클라이언트는 정식 영문 풀네임만 보낸다.

## 1k 동접 관점

- 키 한 번 = 메시지 한 건. 사용자가 키를 빠르게 떼지 않고 누르고 있을 때 OS
  키 리피트가 발생하면 초당 20~30건이 들어올 수 있다. 두 게이트가 이를 흡수한다:
  1. **서버 이동 쿨다운** — `MOVE_COOLDOWN_MS` (기본 500ms) + 큐잉 모델
     (latest-wins). 즉, 30 Hz 키 리피트가 들어와도 실제로 방을 떠나는 것은
     2 Hz 가 상한이고 그 사이 입력은 마지막 방향 하나로 압축된다(command.md
     「move」 절).
  2. **글로벌 토큰 버킷** — 플레이어당 `CMD_RATE_PER_SEC=8`/`CMD_BURST=16`.
     키 리피트 폭주가 위 쿨다운을 우회하지 않더라도 cmd 파이프라인 진입 직후
     소비되어, 디스패처보다 안쪽 비용은 1k × 8 = 8k cmd/s 안에서 잡힌다.
- 클라이언트 측 throttle 은 별도로 두지 않는다 — 서버 큐잉이 latest-wins 라
  중복 입력은 모두 한 발로 압축되고, 토큰 버킷이 부족하면 silent drop 이라
  추가 응답도 없다.

## 향후 확장 후보 (구현 안 됨)

- WASD 외에 화살표 키도 동일하게 매핑.
- 대각선 이동(QE/ZC) — 룸 그래프가 8방위를 지원해야 함. 현재 룸 정의는 4방위만.
- 모드 전환 키를 ESC 외에 추가 (예: Tab). 사용자 피드백 후 결정.
- 이동 중 장애물 / 잠긴 문 응답 시각화 — 현재는 평문 시스템 메시지.

## 변경 이력

- 입력창 ESC → 이동 모드 토글 추가
- WASD → 동서남북 이동 키 매핑 (W=북, A=서, S=남, D=동)
- 클라이언트 단독 변경 — 서버 프로토콜은 그대로
- Enter 글로벌 동작 추가 — 포커스 위치에 무관하게 Enter는 항상 입력 모드(프롬프트 포커스)로 진입. 프롬프트가 이미 포커스 상태면 form submit으로 자연 통과.
