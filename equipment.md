# equipment.md — 장비·아이템 시스템

모든 아이템(소비/장비/잡템)은 `server/game.js` 의 단일 진실원
**`ITEM_DEFS`** 사전에 정의된다. 인벤토리·장비 슬롯에 들어가는 객체는
이 정의의 **스냅샷**(makeItem) — 나중에 `ITEM_DEFS` 의 stat 가 바뀌어도
이미 보유 중인 아이템의 능력치는 그대로 유지된다(보존성).

---

## 아이템 종류

| `kind`     | 설명                                  | 클라 클릭 시 명령        |
|------------|---------------------------------------|--------------------------|
| `consume`  | 사용 시 효과(체력 물약 등)            | `use <id>`               |
| `equip`    | 슬롯에 끼우는 장비(공격/방어 보너스)  | `equip <id>`             |
| `misc`     | 잡템 — 효과/슬롯 없음(밧줄 등)        | `use <id>`(거부 메시지)  |

---

## 슬롯

`player.equipment` 의 키는 다섯 개로 고정:

```
head    머리
body    몸통
weapon  무기
offhand 보조
feet    발
```

`equip` 이 들어오면 아이템의 `slot` 필드가 가리키는 슬롯에 들어가고, 해당
슬롯에 이미 장비가 있었다면 인벤토리로 자동 환원(스왑) — 별도의 `unequip`
선행 호출 필요 없음.

---

## 장비 매트릭스 (현재 정의)

| id              | 이름           | slot     | attack | defense | 비고                    |
|-----------------|----------------|----------|--------|---------|-------------------------|
| `short_sword`   | 단검           | weapon   | 5      | -       | 시작 무기               |
| `tunic`         | 낡은 튜닉      | body     | -      | 1       | 시작 갑옷               |
| `boots`         | 가죽 부츠      | feet     | -      | 1       | 시작 부츠               |
| `goblin_dagger` | 고블린의 단검  | weapon   | 8      | -       | 고블린 드랍             |
| `bone_helmet`   | 뼈 투구        | head     | -      | 3       | 해골 전사 드랍          |
| `bone_sword`    | 뼈 검          | weapon   | 11     | -       | 해골 전사 드랍          |
| `dragon_scale`  | 비룡 비늘 갑옷 | body     | -      | 12      | 검은 비룡 드랍          |
| `dragon_fang`   | 비룡 송곳니 검 | weapon   | 18     | -       | 검은 비룡 드랍          |
| `flame_cloak`   | 불꽃 망토      | body     | -      | 10      | 붉은 비룡 드랍          |
| `flame_blade`   | 불꽃 검        | weapon   | 16     | -       | 붉은 비룡 드랍          |

---

## 데미지 공식

**기본 공격 (PvE / PvP):**
```
dmgOut = random(0..7) + 3 + Σ equipment.attack
```

- 빈손 base: 3 (~10 random) — 옛 「무기 없음 = 3~10」 그대로.
- 시작 단검 장착: 8 (~15) — `short_sword.attack=5` 가 옛 baseline 을 그대로 재현.
- 상위 장비는 attack 합으로 자연스레 비례 증가.

**몬스터 반격 (PvE):**
```
dmgIn = max(1, random(0..atk-1) + 1 - Σ equipment.defense)
```

- 방어구가 충분하면 항상 최소 1 — stalemate 방지.
- 시작 장비(튜닉+부츠)로 방어 2, 비룡 비늘(12) + 뼈 투구(3) + 부츠(1) = 16 까지 누적 가능.

마법은 별도 공식(`SPELL_DEFS[id].dmg`)이며 attack 합과 무관 — magic.md 참조.

---

## 드랍 매트릭스

각 몬스터는 정의에 `drops: [{ id, chance }]` 배열을 가진다. 처치 시 마지막
일격을 가한 한 명이 각 엔트리를 **독립 베르누이 시행**(서로 영향 X)으로 굴려
당첨된 아이템을 인벤토리로 받는다. 알림은 「★ {아이템} 획득!」 시스템 라인.

| 몬스터       | 드랍                                              | 합산 기대값 |
|--------------|---------------------------------------------------|-------------|
| 고블린       | 고블린의 단검 (10%)                               | 0.10        |
| 해골 전사    | 뼈 투구 (8%) · 뼈 검 (5%)                         | 0.13        |
| 검은 비룡    | 비룡 비늘 갑옷 (6%) · 비룡 송곳니 검 (4%)         | 0.10        |
| 붉은 비룡    | 불꽃 망토 (6%) · 불꽃 검 (4%)                     | 0.10        |

새 드랍을 추가할 때:

1. `ITEM_DEFS` 에 아이템 한 줄.
2. 해당 `MONSTER_DEFS[id].drops` 에 `{ id, chance }` 추가.
3. 본 문서의 두 매트릭스에 한 줄씩 갱신.

---

## 명령

### 장착

```
장착 <장비 이름/id>
equip <장비 이름/id>
```

- 인벤토리에서 일치하는 첫 `equip`-kind 아이템을 슬롯으로 옮긴다.
- 슬롯이 차 있으면 자동 스왑.
- 같은 인벤토리 엔트리에 qty>1 이면 1만 빠지고 나머지는 그대로 — 같은 아이템 여러 장 보관 가능.
- `use <장비>` 로 들어와도 서버가 자동으로 equip 으로 라우팅(편의).

### 해제

```
해제 <슬롯명 | 장착품 이름>
unequip <슬롯명 | 장착품 이름>
```

- 슬롯 키워드: `머리/몸통/무기/보조/발` 또는 `head/body/weapon/offhand/feet`.
- 장착품 이름으로도 매칭 — `해제 비룡 비늘 갑옷` 가능.
- 빈 슬롯 호출은 안내 라인.

### 클릭 단축경로

- **인벤토리 li 클릭** — equip-kind 면 `equip <id>`, 그 외엔 `use <id>` 송신.
- **장비 슬롯 li 클릭** — `unequip <slot>` 송신. 빈 슬롯에는 data-slot 이 없어
  무반응.

---

## 클라이언트 표시

사이드바의 장비/인벤토리 li 는 능력치 토큰을 이름 옆에 붙여 표시:

- `+5⚔` — 공격 보너스
- `+3🛡` — 방어 보너스

좁은 사이드바에서 별도 컬럼을 두면 가독성이 떨어져 한 줄에 합쳤다. 인벤토리
의 우측 컬럼은 소비/잡템에는 수량(`×N`), 장비에는 공란(능력치는 이미 이름 옆).

---

## 새 아이템 추가 절차

1. `server/game.js` 의 `ITEM_DEFS` 에 엔트리 추가
   (`id/name/icon/kind` 필수, equip 이면 `slot` + `attack`/`defense`).
2. (선택) `ITEM_USE` 에 사용 효과 함수(consume 한정).
3. 본 문서의 장비/드랍 매트릭스에 한 줄.
4. 드랍이라면 `MONSTER_DEFS[…].drops` 에 `{ id, chance }` 추가.

별도 클라이언트 변경은 필요 없다 — 사이드바 렌더는 ITEM_DEFS 의 어떤 엔트리도
일관되게 그린다.
