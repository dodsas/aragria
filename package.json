{
  "name": "agria",
  "version": "1.0.0",
  "main": "server/index.js",
  "scripts": {
    "test": "node --test 'tests/*.test.js'",
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "DEV=1 node --env-file-if-exists=.env --watch server/index.js",
    "stop": "pkill -f 'node server/index.js' || true",
    "restart": "npm run stop && sleep 0.5 && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3",
    "express": "^5.2.1",
    "ws": "^8.20.0"
  }
}
